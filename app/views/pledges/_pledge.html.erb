<tr>
	<th scope="row"><%= link_to pedge.id %> </th>
	<td><%= link_to pledge.project.name, project_path(pledge.project) %></td>
	<td><%= number_to_currency(pledge.ammount, unit: "$") %></td>
	<td><%= pledge.reward.estimated_delivery %></td>
</tr>